# Sidebar & Toolbar Drag UX Notes

## User Feedback (2026-04-05)

### Sidebar minimized state
- Don't make button background white on hover
- Use a sidebar icon instead of chevron
- Add a drag handle icon (6-dot grid) to make it draggable
- Save position in localStorage

### Toolbar
- Make toolbar also draggable (same drag handle pattern)
- Add bottom and right margin by default (currently flush to edge)
- Save position in localStorage

### Both
- Draggable within the canvas
- Persist positions across sessions via localStorage
</content>
</invoke>