---
phase: 07-open-source-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/start.ts
  - src/cli/utils/output.ts
  - src/main/window.ts
  - package.json
  - LICENSE
  - tests/cli/start.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All CLI spinner text says 'Claw Design' not 'Electron'"
    - "Window title shows 'Claw Design -- {project-name}'"
    - "Splash screen shows 'Claw Design' not 'claw-design'"
    - "Error messages say 'browser component' not 'Electron'"
    - "Version output stays 'clawdesign v0.1.0' (unchanged)"
    - "package.json points to prodoxx/claw-design"
    - "LICENSE copyright is prodoxx"
  artifacts:
    - path: "src/cli/commands/start.ts"
      provides: "Rebranded spinner text and error messages"
      contains: "Claw Design"
    - path: "src/main/window.ts"
      provides: "Rebranded window title and splash screen"
      contains: "Claw Design"
    - path: "package.json"
      provides: "Updated ownership and metadata"
      contains: "prodoxx/claw-design"
    - path: "LICENSE"
      provides: "Updated copyright"
      contains: "prodoxx"
  key_links:
    - from: "src/cli/commands/start.ts"
      to: "tests/cli/start.test.ts"
      via: "test assertions match rebranded error strings"
      pattern: "Browser component not found"
---

<objective>
Rebrand all user-facing strings from "Electron" to "Claw Design" and update package ownership from nebula-core-org to prodoxx.

Purpose: Prepare the tool's identity for public release -- users should see "Claw Design" branding throughout, never internal implementation details like "Electron". Ownership must point to the correct GitHub account.

Output: Rebranded CLI output, window title, splash screen, error messages. Updated package.json, LICENSE with prodoxx ownership.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-open-source-readiness/07-CONTEXT.md

<interfaces>
<!-- Key functions and patterns the executor needs -->

From src/cli/utils/output.ts:
```typescript
export function createSpinner(text: string): Ora;
export function printHeader(version: string): void;
export function printReady(port: number): void;
export function printError(title: string, message: string, suggestion?: string): void;
```

From src/cli/commands/start.ts (lines to change):
```typescript
// Line 35-39: Error message
printError('Electron not found', 'The Electron binary is missing from the installation.', 'Reinstall: npm install -g claw-design');

// Line 205-207: Spinner
const electronSpinner = createSpinner('Opening Electron window...');
const electronProcess = spawnElectron(`http://localhost:${port}`, projectName);
electronSpinner.succeed(`Electron window opened ${pc.dim(`localhost:${port}`)}`);

// Line 225: Exit message
console.log(pc.dim('\n  Electron window closed.'));
```

From src/main/window.ts (lines to change):
```typescript
// Line 140: Window title
title: `claw-design \u2014 ${projectName} \u2014 localhost:${port}`,

// Line 210: Splash brand
<div class="splash__brand">claw-design</div>
```

From tests/cli/start.test.ts (lines 242-255):
```typescript
it('exits when Electron binary missing', async () => {
  // ...
  expect(mockPrintError).toHaveBeenCalledWith(
    'Electron not found',
    'The Electron binary is missing from the installation.',
    'Reinstall: npm install -g claw-design'
  );
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand all user-facing CLI and Electron strings</name>
  <files>src/cli/commands/start.ts, src/cli/utils/output.ts, src/main/window.ts, tests/cli/start.test.ts</files>
  <read_first>
    - src/cli/commands/start.ts (current spinner text, error messages)
    - src/cli/utils/output.ts (printHeader, printReady functions)
    - src/main/window.ts (window title on line 140, splash brand on line 210)
    - tests/cli/start.test.ts (test assertions that will need updating)
  </read_first>
  <action>
Rebrand user-facing strings per D-01 through D-06. Internal variable names and file names stay as-is (per D-06).

**src/cli/commands/start.ts changes:**

1. Line 35-39 -- Change error from "Electron not found" to branded message (per D-04):
   ```typescript
   printError(
     'Browser component not found',
     'A required browser component is missing from the installation.',
     'Reinstall: npm install -g claw-design'
   );
   ```

2. Line 93 comment -- Update comment from "Electron window title" to "Claw Design window title" (code stays same)

3. Line 205-207 -- Change spinner text (per D-01, D-02):
   ```typescript
   const electronSpinner = createSpinner('Starting Claw Design...');
   const electronProcess = spawnElectron(`http://localhost:${port}`, projectName);
   electronSpinner.succeed(`Claw Design ready ${pc.dim(`localhost:${port}`)}`);
   ```
   Note: variable name `electronSpinner` and `electronProcess` stay as-is per D-06.

4. Line 225 -- Change exit message (per D-01, D-04):
   ```typescript
   console.log(pc.dim('\n  Claw Design window closed.'));
   ```

**src/cli/utils/output.ts changes:**

No changes needed. `printHeader` already outputs `clawdesign v${version}` which matches D-05. `printReady` says "Ready!" which is brand-neutral. Leave as-is.

**src/main/window.ts changes:**

1. Line 140 -- Window title (per D-03):
   ```typescript
   title: `Claw Design \u2014 ${projectName} \u2014 localhost:${port}`,
   ```

2. Line 210 -- Splash screen brand text (per D-01):
   ```html
   <div class="splash__brand">Claw Design</div>
   ```

**tests/cli/start.test.ts changes:**

1. Lines 247-250 -- Update the "Electron binary missing" test assertions to match rebranded strings:
   ```typescript
   expect(mockPrintError).toHaveBeenCalledWith(
     'Browser component not found',
     'A required browser component is missing from the installation.',
     'Reinstall: npm install -g claw-design'
   );
   ```

2. Line 242 -- Update test name:
   ```typescript
   it('exits when browser component missing', async () => {
   ```
  </action>
  <verify>
    <automated>cd /Users/nico/code/nebula-core-org/claw-design && npx vitest run tests/cli/start.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/commands/start.ts contains `'Starting Claw Design...'`
    - src/cli/commands/start.ts contains `Claw Design ready`
    - src/cli/commands/start.ts contains `'Browser component not found'`
    - src/cli/commands/start.ts contains `'A required browser component is missing from the installation.'`
    - src/cli/commands/start.ts contains `'Claw Design window closed.'`
    - src/cli/commands/start.ts does NOT contain the string `'Opening Electron window...'`
    - src/cli/commands/start.ts does NOT contain `'Electron not found'` as a user-facing string
    - src/main/window.ts contains `Claw Design \u2014 ${projectName}`
    - src/main/window.ts contains `Claw Design</div>` (splash brand)
    - src/main/window.ts does NOT contain `claw-design</div>` in the splash brand div
    - tests/cli/start.test.ts contains `'Browser component not found'`
    - tests/cli/start.test.ts passes: `npx vitest run tests/cli/start.test.ts` exits 0
    - src/cli/utils/output.ts is unchanged (printHeader still shows `clawdesign v${version}` per D-05)
    - Internal variable names like `electronSpinner`, `spawnElectron`, `electronProcess` are unchanged per D-06
  </acceptance_criteria>
  <done>All user-facing "Electron" references replaced with "Claw Design" or "browser component". Window title and splash screen show "Claw Design". Tests pass with updated assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json ownership and LICENSE copyright</name>
  <files>package.json, LICENSE</files>
  <read_first>
    - package.json (current repository, homepage, author, description fields)
    - LICENSE (current copyright line)
  </read_first>
  <action>
Update ownership metadata per D-07, D-19 through D-23.

**package.json changes:**

1. `description` (per D-07) -- Update to a more compelling npm search description:
   ```json
   "description": "Point at your running website, describe changes in plain English, and watch Claude Code edit the source live"
   ```

2. `repository` (per D-19, D-23):
   ```json
   "repository": {
     "type": "git",
     "url": "https://github.com/prodoxx/claw-design"
   }
   ```

3. `homepage` (per D-19, D-23):
   ```json
   "homepage": "https://github.com/prodoxx/claw-design"
   ```

4. `author` (per D-21):
   ```json
   "author": {
     "name": "prodoxx",
     "url": "https://github.com/prodoxx"
   }
   ```

5. `name` stays `"claw-design"` (per D-22, unscoped).

6. All other fields (version, bin, scripts, dependencies, etc.) stay unchanged.

**LICENSE changes:**

1. Line 3 -- Change copyright holder (per D-20):
   ```
   Copyright (c) 2026 prodoxx
   ```
   Everything else in the LICENSE file stays the same.
  </action>
  <verify>
    <automated>cd /Users/nico/code/nebula-core-org/claw-design && node -e "const p=require('./package.json'); console.log('repo:', p.repository.url); console.log('home:', p.homepage); console.log('author:', JSON.stringify(p.author)); console.log('desc:', p.description); console.log('name:', p.name);" && head -3 LICENSE</automated>
  </verify>
  <acceptance_criteria>
    - package.json `repository.url` equals `https://github.com/prodoxx/claw-design`
    - package.json `homepage` equals `https://github.com/prodoxx/claw-design`
    - package.json `author.name` equals `prodoxx`
    - package.json `author.url` equals `https://github.com/prodoxx`
    - package.json `name` equals `claw-design` (unchanged, per D-22)
    - package.json `description` does NOT equal `Visual web development tool powered by Claude Code` (old text)
    - package.json `description` contains "Point at" or similar compelling language per D-07
    - LICENSE line 3 contains `Copyright (c) 2026 prodoxx`
    - LICENSE does NOT contain `nebula-core-org`
  </acceptance_criteria>
  <done>package.json points to prodoxx/claw-design with compelling description and author object. LICENSE copyright updated to prodoxx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json metadata | Public npm registry reads this; no secrets, only public URLs |
| CLI output strings | Shown in user terminal; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | CLI error messages | mitigate | D-04: Replace "Electron" references so internal technology stack is not exposed to end users |
| T-07-02 | Spoofing | package.json repository URL | accept | URLs point to public GitHub repo; npm publish requires auth token regardless of metadata |
</threat_model>

<verification>
1. `npx vitest run tests/cli/start.test.ts` passes (rebranded error strings match assertions)
2. `grep -r "Opening Electron" src/` returns no matches (all user-facing strings rebranded)
3. `grep "Electron not found" src/cli/` returns no matches
4. `grep "prodoxx" package.json LICENSE` returns matches in both files
5. `grep "nebula-core-org" package.json LICENSE` returns no matches
</verification>

<success_criteria>
- All user-facing CLI strings say "Claw Design" instead of "Electron" (D-01, D-02, D-04)
- Window title format is `Claw Design -- {project-name}` (D-03)
- Version output stays `clawdesign v0.1.0` (D-05)
- Internal code (variable names, file names) unchanged (D-06)
- package.json ownership points to prodoxx/claw-design (D-19, D-23)
- LICENSE copyright is prodoxx (D-20)
- Tests pass with updated assertions
</success_criteria>

<output>
After completion, create `.planning/phases/07-open-source-readiness/07-01-SUMMARY.md`
</output>
